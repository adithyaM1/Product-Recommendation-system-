# Copy this file to ".env" if you want Claude to rephrase recommendation
# reasons in a friendlier tone. This is entirely OPTIONAL -- the agent
# produces solid, rule-based reasons on its own with no API key at all.

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-20241022
